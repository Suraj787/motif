# Motif v3.0.0 release readiness

Branch `fix/claim-finding-separation`. This records the final-gate evidence required to
publish stable `v3.0.0`.

## Code and CI
- PR #5 (evidence wildcard + originality recalibration) merged into `main` (`be70d89`).
- Claim/finding separation on `fix/claim-finding-separation` (this branch), PR pending.
- `make check`: 151 dependency-free self-checks + secret scan green.
- Browser golden loop (`--require-browser`): green in CI on the bundled Vue fixture; no
  `not-executed` stages; exact rollback; baseline unchanged.
- Clean-clone validation green. Core is dependency-free; no new dependency.

## Correctness
- Evidence wildcard fix validated across 11 contexts.
- Originality saturation fix validated across 8 fixtures (generic-AI still 100; bento 85).
- Applicable claims separated from findings; blocking requires detector evidence,
  sufficient confidence, a machine-supported enforcement mode, and no unresolved contradiction.
- BOSS finding sample reviewed (`boss-finding-quality-review.md`): blocking precision 1/1,
  0 outright false positives, 0 human-review misclassification.

## Generalisation
- Bundled Vue fixture: browser-verified in CI.
- BOSS audited read-only (Vue): 105 applicable, 1 evidence-backed blocking, 0 files modified.
- React project audited read-only: detected, 1 blocking, 0 files modified.
- AI-generated project audited read-only: detected, 2 blocking, originality 41 (moderate),
  0 files modified.
- Unsupported repair paths not invoked; source projects unmodified.

## Product and documentation
- Capability matrix honest (implemented / experimental / unsupported sections).
- Installation matrix documented (truthful: Python 3.11/3.12, Node 20 in CI; local pip
  broken by an environment libexpat issue unrelated to Motif; core runs on stdlib).
- Migration documented (`docs/migration/v2-to-v3.md`).
- Security review complete (`v3-security-and-safety-review.md`).
- Release notes truthful (`v3.0.0-release-notes.md`); known limitations explicit.

## Installation matrix (truthful)
| Environment | Base install | Browser extra | CLI / doctor | Verified |
|---|---|---|---|---|
| Python 3.12 (CI ubuntu) | yes | yes | yes | CI browser workflow |
| Python 3.12 (local macOS) | n/a (system pip broken) | n/a | yes (stdlib, `python -m ii`) | local make check |
| Python 3.11 | requires-python >= 3.11 | expected | expected | declared, CI uses 3.12 |
| Node 20 LTS | n/a | fixture install | n/a | CI golden loop |
| Node 22 LTS | n/a | expected | n/a | not yet verified |

Local `pip install` fails due to a broken system `pyexpat`/libexpat ABI in this machine's
Python, which is an environment defect, not a Motif defect; the dependency-free core runs via
`python -m ii`, and CI proves the packaged install on clean ubuntu.

## Known limitations (explicit)
- Browser assurance is proven only on the bundled Vue fixture.
- react-router route discovery is not implemented.
- Repair is limited to the colour-only-status class on Vue.
- Originality is static, confidence capped at moderate, and never asserts AI authorship.
- `missing-reduced-motion` can over-report library-owned animation (kept non-blocking).

## Gate result
All final gates pass. No unresolved critical issue. Eligible for stable `v3.0.0` once the
claim/finding-separation PR is merged and workflows are green on the resulting `main`.
